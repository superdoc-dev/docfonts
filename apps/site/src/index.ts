/**
 * docfonts.dev site (scaffold). Static-generated from @docfonts/registry:
 *   /fonts/<proprietary-font>        ONE canonical page per proprietary font (the SEO surface)
 *   /guides/missing-fonts-in-docx
 *   /guides/metric-compatible-fonts
 *   /tools/scan-docx-fonts           client-side scanner (parses in-browser, never uploads)
 *
 * Framework: Bun + React 19 + prerender, mirroring docx-tools/apps/web. SuperDoc connection is
 * surfaced secondarily and stays honest ("Built by the team behind SuperDoc") until the resolver
 * actually consumes the registry. TODO: implement.
 */
export {};
