/**
 * Build static page data for apps/site from @docfonts/registry records: one canonical page per
 * proprietary font (no split /fonts and /pairs - avoid keyword cannibalization), plus the guides
 * and the client-side scanner. TODO: implement.
 */
export {};
