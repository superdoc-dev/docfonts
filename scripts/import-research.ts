/**
 * One-way import of VERIFIED artifacts from ../font-fidelity-research into @docfonts/registry
 * evidence records (reconciled catalog rows, measured advance deltas, proofs, source inventory).
 *
 * Read-only from the research repo. Never copies proprietary fonts. Nothing becomes metric-safe
 * without a measurement carried over from the research artifacts. TODO: implement.
 */
export {};
